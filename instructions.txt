To run it locally:
Create a directory for databse. Recommend ``db``
Thus,
example.
```
mkdir db
```
Then, run.
```
mongod --dbpath ./${Directory you named}
```
In my case,
```
mongod --dbpath ./db
```
