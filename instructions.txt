To run it locally:
Create a directory for databse. Recommend ``db``
Thus,
example.
```
mkdir db
```
Then, run.
```
mongodb --dbpath ./${Directory you named}
```
In my case,
```
mongodb --dbpath ./db
```
In the case for ubuntu
```
mongod --dbpath ./db
```
